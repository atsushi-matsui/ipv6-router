# ipv6-router

## Installation

- up docker container
  ```sh
  docker compose watch
  ```

## Usage in docker container
- setup network env
  ```sh
  ./scripts/init.sh
  ```
- build
  ```sh
  make build
  ```
- run
  ```sh
  make run
  ```
